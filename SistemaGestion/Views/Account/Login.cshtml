@model SistemaGestion.Models.ViewModels.UserLoginView
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Login.cshtml";
}

<div class="container h-100">
    <div class="row justify-content-sm-center h-100">
        <div class="col-xxl-4 col-xl-5 col-lg-5 col-md-7 col-sm-9">
            <div class="text-center my-5">
                <img src="~/Content/img/fp-logo.png" class="img-fluid">
            </div>
            <div class="card shadow-lg">
                <div class="card-body pt-3 pr-5 pl-5">
                    <h2 class="fs-4 card-title fw-bold mb-4 text-center">Sistema de Gestión</h2>
                    <h2 class="fs-4 card-title fw-bold mb-4 text-center">Produccion</h2>
                    @*<form method="POST" class="needs-validation" novalidate="" autocomplete="off">
            <div class="mb-3">
                <label class="mb-2 text-muted" for="name">Nombre de Usuario</label>
                <input id="nameuser" type="text" class="form-control" name="nameuser" value="" required autofocus>
                <div class="invalid-feedback">
                    Email is invalid
                </div>
            </div>
            <div class="mb-3">
                <div class="mb-2 w-100">
                    <label class="text-muted" for="password">Contraseña</label>
                </div>
                <input id="password" type="password" class="form-control" name="password" required>
                <div class="invalid-feedback">
                    <div id="mensaje1" class="errores">Dame Tu password></div>
                    Password is required
                </div>
            </div>
            <div class="d-flex align-items-center">
                <button id="btcarga" type="button" class="btn btn-sm btn-danger btn-block"><i class="fas fa-search"></i> Ingresar</button>

            </div>
        </form>*@

                    @using (Html.BeginForm("LogIn", "Account", FormMethod.Post, new { @class = "form-signin" }))
                    {
                        @Html.AntiForgeryToken()
                        <div class="mb-3">
                            <label class="mb-2 text-muted" for="name">Nombre de Usuario</label>
                            @Html.TextBoxFor(model => model.LoginName, new { @class = "form-control", @type = "LoginName", @placeholder = "USUARIO" })
                            @Html.ValidationMessageFor(model => model.LoginName)
                        </div>

                        <div class="mb-3">
                            <div class="mb-2 w-100">
                                <label class="text-muted" for="password">Contraseña</label>
                            </div>
                            @Html.TextBoxFor(model => model.Password, new { @class = "form-control", @type = "password", @placeholder = "PASSWORD" })
                            @Html.ValidationMessageFor(model => model.Password)
                        </div>


                        <input type="submit" value="INGRESAR" class="btn btn-sm btn-danger btn-block" />
                    }
                    <div class="form-group mb-0 mt-2" style="font-size:13px;">
                        @Html.ValidationSummary(true, "", new { @class = "list-group alert alert-danger text-initial mb-1 " })
                    </div>
                </div>
                @*<div class="card-footer mb-0">
                    
                </div>*@
            </div>
            <div class="text-center mt-5 text-muted">
                Copyright &copy; 2017-2022 &mdash; FashionPark
            </div>
        </div>
    </div>
</div>
@*</section>*@

@*@section scripts{
        <script>
            $(function () {
                $(document).ready(function () {
                })

                $("#btcarga").click(function () {
                    event.preventDefault();
                    var url = "@Url.Action("Login", "Account")";
                    var manejarErrorAjax = "Eror";
                    var Usuario = $("#nameuser").val();
                    var Contra = $("#password").val();
                    var data = {users: Usuario, pass: Contra};
                    $.post(url,data).done(function (data) {
                        if (data =="1") {
                            /*swal("Se Cargo Correctamente!", "Presiona OK Para Continuar", "success");*/
                            document.location.href = "@Url.Content("~/Admin/Dash")";
                        }else{
                            swal("Usuario Invalido", "Presiona OK Para Continuar", "warning");
                        }
                        $("mensaje-alerta").html(data)
                    }).fail(manejarErrorAjax);


                });

            })
        </script>

    }*@

