
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<body class="sb-nav-fixed">
    <div id="layoutSidenav">
        <div id="layoutSidenav_content">
            <main>
                <div class="container-fluid px-4">


                    <div class="row">
                        <div class="col-sm-12">
                            <div class="card border-secondary">
                                <div class="card-header bg-secondary text-white">
                                  Libro Mayor/ConciliacionAutomatica
                                </div>

                                <div class="card-body p-2">
                                    <div class="row">

                                        <div class="col-sm-3">
                                            <div class="form-group mb-0">
                                                <label for="btnBuscar" class="col-form-label col-form-label-sm invisible">Direccion:</label>
                                                <button type="button" data-toggle="modal" id="VistaParcialLV" data-target="#Using" class="btn  btn-warning btn-block">
                                                    <i class="fas fa-upload"></i> N°1-Cargar Libro
                                                </button>
                                            </div>

                                        </div>
                                        <div class="col-sm-3">
                                            <div class="form-group mb-0">
                                                <label for="btnBuscar" class="col-form-label col-form-label-sm invisible">Direccion:</label>
                                                <button type="button" data-toggle="modal" id="ConciliacionAuto" data-target="#Using" class="btn btn-primary btn-block">
                                                    <i class="fa fa-cubes"></i> N°2-GenerarConciliacionAuto
                                                </button>
                                            </div>

                                        </div>

                                        <div class="col-sm-3">
                                            <div class="form-group mb-0">
                                                <label for="btnBuscar" class="col-form-label col-form-label-sm invisible">Direccion:</label>
                                                <button type="button" data-toggle="modal" id="Descarga" data-target="#Using" class="btn btn-success btn-block">
                                                    <i class="fas fa-download"></i> N°3-DescargarConciliacionAuto
                                                </button>
                                            </div>

                                        </div>
                                        <div class="col-sm-3">
                                            <div class="form-group mb-0">
                                                <label for="btnBuscar" class="col-form-label col-form-label-sm invisible">Direccion:</label>
                                                <button type="button" data-toggle="modal" id="ConcliacionManual" data-target="#Using" class="btn btn-danger btn-block">
                                                    <i class="fa fa-cogs"></i> N°4-ConciliacionManual
                                                </button>
                                            </div>

                                        </div>

         

                                    </div>




                                    <div class="row mb-2">

                                    </div>

               
                                </div>
                            </div>
                            <!-- Modal -->

                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>
</body>

<div class="modal fade" id="modal" data-backdrop="static" tabindex="-1" role="dialog" aria-labelledby="staticBackdropLabel" aria-hidden="true">
</div>
<div class="modal fade" id="ModalDetalle" role="dialog">
</div>

@section scripts{

    <script>
        $(document).ready(function () {
            $('#tbNCIDetalleByCenco').DataTable(
                {
                    responsive: true,

                    "language": {
                        url: "@Url.Content("~/Content/datatable/js/datatable_spanish.json")"
                    }
                });

            $('#Descarga').click(function (event) {
            event.preventDefault();
            var url = '@Url.Action("ExcelConciliadasA", "CuadraturaEFO")';
        
            var num = 1;
            DownloadFileFechasEntre(url,num);
        });




            $('#VistaParcialLV').click(function () {
                var result = confirm("Desea Cargar El libro mayor?.. Si Carga nuevamente el libro eliminara los datos anteriormente cargados");
                if (result) {
                    var url = '@Url.Action("CargaLibroMayor", "CuadraturaEFO")';
                    $.ajax({
                        type: 'GET',
                        url: url,
                        async: false,
                        processData: false,
                        contentType: false,
                        success: function (response) {
                            $("#modal").html(response);
                            $("#modal").modal('show');


                        },
                        error: function (xhr, ajaxOptions, thrownError) {
                            alert(thrownError);
                        }
                    });
                }

            });

            $('#ConciliacionAuto').click(function () {
                event.preventDefault();
                var url = '@Url.Action("ConciliacionAutomatica", "CuadraturaEFO")';
                var result = confirm("Desea Conciliar Automaticamente?");
                var manejarErrorAjax = "Eror";
                if (result) {
                    $('#cargal').modal('show');

                    $.post(url).done(function (response) {

                        if (response.error == false) {
                            setTimeout(mostrarMensaje, 2000);
                            setTimeout(Subir, 4000);
                            function mostrarMensaje() {
                                $("#cargarMensajes").modal('show');
                                $('#mensajeLoad').html(response.respuesta);
                                console.log("mostrarMensaje");

                            };
                            function Subir() {
                                $('#cargarMensajes').modal('hide');
                                $('#cargal').modal('hide');

                            };



                        } else {
                            setTimeout(mostrarMensaje, 4000);
                            setTimeout(Subir, 2000);
                            console.log("Entre en el else");
                            function Subir() {
                                $('#cargarMensajes').modal('hide');
                                $('#cargal').modal('hide');


                            }

                            function mostrarMensaje() {
                                $("#cargarMensajes").modal('show');
                                $('#mensajeLoad').html(response.respuesta);

                            };

                        }


                    }).fail(manejarErrorAjax);

                };
            });







            });


        function DownloadFileFechasEntre(urlAc,num) {


            /*var url = urlAc;*/
            //var Fecha = F;
            //var fechaFin = fechaT;
            //var tipoR = tipo;

            var list = [];
            var fileName = '';
            list.push({ fileName: fileName });

            $('#cargal').modal('show');
            $.ajax({
                url: urlAc,
                type: "post",
                data: { num: num},
                xhrFields: {
                    responseType: 'blob'
                },
                success: function (data, textStatus, xhr) {
                    var filename = list[0].filename;
                    var disposition = xhr.getResponseHeader('Content-Disposition');
                    if (disposition && disposition.indexOf('attachment') !== -1) {
                        var filenameRegex = /filename[^;=\n]*=((['"]).*?\2|[^;\n]*)/;
                        var matches = filenameRegex.exec(disposition);
                        if (matches != null && matches[1]) filename = matches[1].replace(/['"]/g, '');
                        var a = document.createElement('a');
                        var url = window.URL.createObjectURL(data);
                        a.href = url;
                        a.download = filename;
                        document.body.append(a);
                        a.click();
                        a.remove();
                        window.URL.revokeObjectURL(url);
                    }
                    else {
                        getErrorToastMessage("ERROR AL DESCARGAR ARCHIVO");
                    }
                },
                error: function (XMLHttpRequest, textStatus, errorThrown) {

                },
                complete: function () {
                    //var interfaz = list[0];
                    //deleteArchivo(list);
                    setTimeout(function () {
                        $("#cargal").modal('hide');
                    }, 1500);

                }
            });
        }
    </script>




}


